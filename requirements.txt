streamlit>=1.35.0
# Use modern crewai 1.x+ (compatible with Python 3.11). The old <0.150 pins were from early 2025; now 1.x is current and required for latest tool decorator location (crewai.tools).
crewai>=1.0.0
crewai-tools>=1.0.0
litellm>=1.0.0
ddgs>=6.0
duckduckgo-search>=6.0  # fallback for compatibility
gTTS>=2.5
moviepy>=1.0.3
Pillow>=10.0
google-api-python-client>=2.0
google-auth-httplib2
google-auth-oauthlib
oauth2client>=4.0  # for YouTube OAuth helper
beautifulsoup4>=4.12
requests>=2.32
lxml>=5.0
python-dotenv>=1.0
# For local embeddings if memory used
# sentence-transformers
# For advanced, but optional